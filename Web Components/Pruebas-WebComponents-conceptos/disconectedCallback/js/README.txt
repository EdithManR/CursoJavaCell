Este componente ilustra la forma en la que funciona el ciclo de vida del componente
- La primera fase:
  Es el Constructor, se cargan los datos o variables que utilizaremos dentro de nuestro componente. Esta información se carga en memoria
-La segunda fase es :
  connectedCallback, en esta fase, se agregan los elementos al DOM se renderiza el contenido
-La tercera fase es:
  disconnectedCallback, es la fase en la que se eliminan elementos del DOM, para liberar memoria, y ayudar a mejorar el performance.

